name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Added pyyaml, types-PyYAML, scipy, and redis to match your current project needs
        pip install ruff mypy pytest pydantic confluent-kafka email-validator pyyaml types-PyYAML scipy redis

    - name: Lint with ruff
      run: ruff check .

    - name: Type check with mypy
      run: mypy simulator pipeline tests --install-types --non-interactive

    - name: Run tests
      run: |
        export PYTHONPATH=$PYTHONPATH:.
        # This now runs test_event_schema.py, test_agents.py, and test_features.py
        python -m pytest tests/unit/ -v